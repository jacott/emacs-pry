
* Make key-maps change depending on cursor location to command prompt.

* Better spliting of shell arglist strings to list/array

* Breakpoint in pry
   Use buffer properties to mark; add symbol like gdb

* require_relative interception support for pry
   And load support?

* Exception highlighting and clickable

* Customize pryrc loading: nil, t, string

* Eval exp in pry
   Send the current sexp to the *pry* buffer for evalulation. 
   Do a "!\n" if pry-input not empty - look at proccess-mark, and displayed prompt to determine.



DONE

* Re-aligning process-mark with point using left and right if on input line.

* Auto whereami buffer viewing
   Visit one and only one buffer unless buffer already open but always move to point

